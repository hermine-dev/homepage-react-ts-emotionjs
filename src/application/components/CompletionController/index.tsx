import { Completion } from "../../../presentation/components";
import { FC } from "react";

export const CompletionController: FC = () => {
  return <Completion />;
};

export default CompletionController;
